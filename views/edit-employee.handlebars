<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Editar Funcionário</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container">
    <header class="header">
      <h1>Editar Funcionário</h1>
      <a href="/" class="btn-back">← Voltar</a>
    </header>

    <form action="/team/updateemployee" method="POST" class="form-edit">
      <!-- Coloque o campo oculto DENTRO do form -->
      <input type="hidden" name="id" value="{{employee.id}}">

      <label for="name">Nome:</label>
      <input type="text" name="name" value="{{employee.name}}" required>

      <label for="role">Cargo:</label>
      <input type="text" name="role" value="{{employee.role}}" required>

      <label for="email">Email:</label>
      <input type="email" name="email" value="{{employee.email}}" required>

      <label for="phone">Telefone:</label>
      <input type="tel" name="phone" value="{{employee.phone}}" required>

      <label for="start_date">Data de Entrada:</label>
      <input type="date" name="start_date" value="{{employee.start_date}}" required>

      <label for="status">Status:</label>
      <select name="status" required>
        <option value="true" {{#if employee.status}}selected{{/if}}>Ativo</option>
        <option value="false" {{#unless employee.status}}selected{{/unless}}>Inativo</option>
      </select>

      <button type="submit" class="btn-submit">Salvar Alterações</button>
    </form>
  </div>
</body>
</html>
